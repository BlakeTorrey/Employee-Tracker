{
  "name": "employee-tracker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/app.js",
    "db:create": "psql -U postgres -f db/schema.sql",
    "db:reset": "npm run db:create && npm run db:seed"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "inquirer": "^8.2.4",
    "pg": "^8.13.1"
  }
}
